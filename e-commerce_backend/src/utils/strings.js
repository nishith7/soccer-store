const userEndpoints = {
    post: "/user",
    login: "/login",
};

const productEndpoints = {
    post: "/addProduct",
    postAll: "/bulkAdd",
    getCategory: "/products",
}

module.exports = {
    userEndpoints,
    productEndpoints,
}