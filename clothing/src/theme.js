import { createTheme } from "@mui/material";

const theme = createTheme({
    // status: {
    //   danger: orange[500],
    // },
    palette:{
        primary:
        {
            main:"#F1F9F2",
        }
    }
  });

  export default theme;